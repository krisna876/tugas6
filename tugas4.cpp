#include <stdio.h>
int main ()
{
	int speed = 2; // meters per second
	int time = 100; // second * time;
	int distance = speed * time;
	printf("Distance covered: %d meters\n", distance);
	
	return 0;
}
